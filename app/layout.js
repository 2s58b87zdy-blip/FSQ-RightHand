import './styles.css';
export const metadata = { title: 'FSQ Right Hand', description: 'FSQ digital control center' };
export default function RootLayout({ children }) {
  return <html lang="da"><body>{children}</body></html>;
}
