FSQ COMMAND 1.0 RC2 SECURE

1. Configure AUTH_SECRET (32+ random characters) in Azure App Service.
2. Configure INITIAL_OWNER_PASSWORD (12+ unique characters). Do not reuse the old shared password.
3. Confirm SQL and Blob Storage settings described in README.md.
4. Upload/replace the repository files and commit to main.
5. Wait for GitHub Actions: security check, audit, build and deploy must all pass.
6. Sign in as the initial Owner.
7. Assign every required user a unique password and reactivate migrated accounts as needed.
8. Test database diagnostics, blob diagnostics, Project Binder and one job-photo upload.

Never place Azure, SQL, OpenAI or authentication secrets in source files, chat or screenshots.

